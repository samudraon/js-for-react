const func1 = (x,y) => x * 2;
console.log(func1(3));

const ar = (x, y) => {
    const total = x + y + 10;
    const grandTotal = total * 2;
    return grandTotal;
}

const result = ar(2, 2);
console.log(result);